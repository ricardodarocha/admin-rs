{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "Local",
            "database": "${workspaceFolder:admin}/my_database.db"
        }
    ]
}